{
  "name": "node-codef",
  "version": "0.1.0",
  "description": "NodeJS port of CODEF (Canvas Oldschool Demo Effects Framework)",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp"
  },
  "author": "Bennett Gunson",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^5.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-uglify": "^3.0.2"
  }
}
